// Neutral placeholder favicon for new projects, so the browser's favicon request
// doesn't 404 (a console error in every fresh preview). The agent replaces it with
// the real brand icon.
export const PLACEHOLDER_FAVICON_SVG = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect width="32" height="32" rx="8" fill="#111827"/><circle cx="16" cy="16" r="6" fill="#f9fafb"/></svg>
`;
